[build]
  publish = "."
  functions = "netlify/functions"

# (optional) CORS header for index if you ever load local assets cross-origin
[[headers]]
  for = "/index.html"
  [headers.values]
    Access-Control-Allow-Origin = "*"
